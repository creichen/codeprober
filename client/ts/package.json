{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/**/*.js",
    "test:watch": "tsc -p tsconfig.test.json --watch --preserveWatchOutput & node --test --watch dist-test/test/**/*.js",
    "build": "tsc && terser ../public/ts/main.js -o ../public/ts/main.js -c -m",
    "bw": "tsc --watch",
    "serve": "ts-node ./src",
    "start": "node ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.43.1",
    "ts-node": "^10.7.0",
    "typescript": "^5.9.2"
  }
}
